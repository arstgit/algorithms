function ALLOCATE_OBJECT() {
    if(free == null) throw 'out of space';
    else x == free; free = x.next; return x;
}

function FREE_OBJECT(x) {
    x.next = free;
    free = x;
}


//????????????????????????