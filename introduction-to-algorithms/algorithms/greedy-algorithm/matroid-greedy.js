function GREEDY(M, w) {
    //let A = null;
    //sort M into monotonically decreasing order by weight w
    //for each x in M.S, taken in monotonically decreasing order by weight w(x)
      //if (A U {x} belong to M.T)
        //A = A U {x}
    return A
}